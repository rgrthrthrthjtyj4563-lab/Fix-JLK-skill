# Execution Rules

## Output Checks
- Must include `问卷调研服务结算 / 目录 / 前言 / 报告大标题 / 4.x / 5.x / 附件`; the report ends at 附件2 and must not contain a disclaimer page.
- `目录` must appear before `前言`
- Must preserve header text and TOC field
- Must not include old GKTJ headings
- Charts must follow configured template style profiles
- Tables in result analysis must use template blue-fill styling
